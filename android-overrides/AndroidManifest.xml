<?xml version="1.0" encoding="utf-8"?>
<!--
  AndroidManifest.xml para WeLiftTogether
  Este archivo vive en /android-overrides (versionado en git) porque
  android/ se regenera en cada build de CI vía `npx cap add android` /
  `npx cap sync` y NO se commitea (ver .gitignore). El workflow de
  GitHub Actions copia este archivo sobre el manifest autogenerado
  después de sincronizar, para no perder los permisos nativos que la
  app necesita (cámara, vibración, almacenamiento).

  Los recursos referenciados como @xml/... (backup_rules,
  data_extraction_rules) viven en /android-overrides/xml — versionados
  por la misma razón — y el mismo workflow los copia a
  android/app/src/main/res/xml/ junto con este manifest.

  Si agregas un nuevo plugin de Capacitor que requiera un permiso
  nuevo, agrégalo aquí — no directamente en android/app/.../AndroidManifest.xml,
  porque ese archivo se descarta en cada sync.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- ── Permisos requeridos ─────────────────────────────── -->

  <!-- Hápticos nativos (vibración) -->
  <uses-permission android:name="android.permission.VIBRATE" />

  <!-- Cámara (CameraModal) -->
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-feature android:name="android.hardware.camera" android:required="false" />
  <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

  <!-- NOTA: no se declaran READ/WRITE_EXTERNAL_STORAGE a propósito. Toda la
       data de la app (fotos, feedback, etc.) vive en Directory.Data de
       Capacitor Filesystem, que es almacenamiento interno privado y no
       requiere ningún permiso de almacenamiento. La app nunca toca
       Directory.External ni el picker de archivos necesita este permiso
       en Android moderno — agregarlo solo sugeriría, incorrectamente, que
       la app lee/escribe en almacenamiento compartido. -->

  <!-- Internet (Supabase API) -->
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <application
    android:allowBackup="true"
    android:fullBackupContent="@xml/backup_rules"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/AppTheme"
    android:usesCleartextTraffic="true">

    <activity
      android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
      android:exported="true"
      android:label="@string/title_activity_main"
      android:launchMode="singleTask"
      android:name=".MainActivity"
      android:theme="@style/AppTheme.NoActionBarLaunch"
      android:windowSoftInputMode="adjustResize">

      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>

    </activity>

    <provider
      android:name="androidx.core.content.FileProvider"
      android:authorities="${applicationId}.fileprovider"
      android:exported="false"
      android:grantUriPermissions="true">
      <meta-data
        android:name="android.support.FILE_PROVIDER_PATHS"
        android:resource="@xml/file_paths" />
    </provider>

  </application>

</manifest>
